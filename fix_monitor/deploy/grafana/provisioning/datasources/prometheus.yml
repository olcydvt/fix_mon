apiVersion: 1
datasources:
  - name: Prometheus
    # uid must match the datasource uid baked into the dashboard JSON
    uid: fixmon-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
